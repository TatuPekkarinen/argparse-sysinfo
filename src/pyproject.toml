[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "coremon-sysinfo"
version = "0.1.0"
dependencies = [
    "psutil"
]

[tool.setuptools]
py-modules = ["main", "parameters"]

[project.scripts]
coremon = "main:main"
